{
  "name": "parrot669",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:e2e": "playwright test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:regression-a": "wrangler deploy --env regression-a",
    "deploy:regression-b": "wrangler deploy --env regression-b"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "jsdom": "^26.1.0",
    "wrangler": "^4.102.0"
  }
}
