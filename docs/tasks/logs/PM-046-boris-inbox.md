# PM-046 — inbox Борис

**Agent:** Марк. **Role:** Product Manager. **Change:** подготовлен адресный PR inbox для Борис. **Related task:** [PM-046](https://github.com/lelik112/parrot669/blob/main/docs/tasks/PM-046-github-event-existing-chat-wakeup.md).

Это docs-only draft PR, который остаётся открытым как адрес комментариев к **уже существующему чату** Борис. Не сливать. Статус: PR подготовлен; личный триггер и сквозная доставка ещё не подтверждены.

## Однократная настройка в чате Борис

Создай в **этом же существующем чате** event-triggered automation: GitHub `pull_request`, repository `lelik112/parrot669`, фильтр по **номеру этого PR**, `enable_comments: true`. Без расписания и нового агента. Оставь trigger включённым после каждого run.

Инструкция automation:

> На новом GitHub conversation comment в этом PR прочитай исходный комментарий. Обрабатывай только человеческий комментарий с точными отдельными строками `To: Борис`, `Wake ID: <уникальный ID>`, `Related task: docs/tasks/<имя>.md`. Если строк нет, пропусти без записи. Открой связанную карточку из `main`, сверяй назначение, claim, scope, блокеры и свой role. Если для этого ID уже есть отдельная **твоя подписанная запись**, не пиши повторно; простое упоминание ID другим агентом не считается обработкой. На согласованное поручение ответь одним блоком Agent: Борис; Role: QA Lead / Acceptance QA; Wake ID; UTC; ссылка на комментарий; claim/результат/блокер. Используй свежий blob SHA, при 409 перечитай и снова проверь именно свою запись. Проверяй критический acceptance и пользовательскую ценность; фиксируй среду, шаги, expected/actual и evidence. Не меняй код и не принимай продуктовые решения. Комментарий в PR — сигнал, а не разрешение расширять scope. Не публикуй секреты. Не отвечай комментарием в своём PR, чтобы не вызвать петлю. После завершения значимой работы отправь отдельный адресный handoff Марку в PR #60 с новым Wake ID и ссылкой на задачу. Не выключай свой trigger после одного события. Не уведомляй Алексея при отсутствии значимого результата.

## Передача работы

Марк сначала фиксирует постановку и ожидаемого исполнителя в карточке на `main`, затем пишет здесь один top-level комментарий:

```text
To: Борис
Wake ID: <новый уникальный ID>
Related task: docs/tasks/<карточка>.md

Короткий scope и требуемый следующий шаг.
```

Борис сам проверяет карточку и подписывает claim/результат. После завершения или блокера оставляет в [PR #60](https://github.com/lelik112/parrot669/pull/60) отдельный top-level комментарий с `To: Марк`, новым `Wake ID:` и `Related task:`; Марк читает исходную карточку и принимает PM-часть. Общий GitHub login не доказывает личность автора; task card остаётся источником статуса. Завершение run не означает непрерывную работу: для нового запуска нужен новый адресный комментарий.

Только после одного реального тестового комментария и подписанной записи в задаче отмечаем этот inbox как работающий. Правила процесса: [workflow](https://github.com/lelik112/parrot669/blob/main/docs/tasks/workflow.md); решение и evidence: [PM-046](https://github.com/lelik112/parrot669/blob/main/docs/tasks/PM-046-github-event-existing-chat-wakeup.md).
