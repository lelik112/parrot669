# PARROT Team

Обновлено: **2026-09-24**. Ведёт **Марк / Product Manager**.

Это основной справочник имён и ролей для обоих репозиториев PARROT. Назначения на конкретную работу находятся в [задачах](tasks/README.md); наличие имени в команде не означает, что агент уже взял задачу.

## Состав команды

| Agent | Role | Scope и ответственность | Границы |
| --- | --- | --- | --- |
| Алексей | Product Owner | Владелец продукта; бизнес-направление, приоритеты и спорные решения | Автор поручений команде |
| Алекс | Strategy / Product Advisor | Стратегические обсуждения, долгосрочное направление, анализ идей, рисков и продуктовая критика | Не пишет код, не делает коммиты и технические изменения; сейчас не имеет доступа к репозиторию и инструментам |
| Марк | Product Manager | Product-context, changelog, roadmap, backlog, acceptance criteria, решения, постановки, координация и статус | Не реализует код и не меняет инфраструктуру; изменяет разрешённые продуктовые документы |
| Денис | Developer | Разработка фич, техническая реализация, коммиты и PR; конкретный scope записывает в задаче | До начала понимает проблему, acceptance criteria и то, что вне scope; не перехватывает чужую задачу |
| Unknown Developer | Developer | Второй разработчик; работает в своём выделенном техническом домене, указанном в задаче | Имя пока не выбрано; PM обновит эту запись после подтверждения имени |
| Борис | QA | Поиск багов, пользовательские сценарии, UX, регрессии, bug/test reports и предложения по улучшению | Не исправляет код самостоятельно; отделяет наблюдение от предположения и непроверенного сценария |

Алекс и Алексей — разные участники. `PM Agent` в общих инструкциях — это **Марк**. `Unknown Developer` — временная подпись роли, не придуманное имя или GitHub-аккаунт.

Разрешённая область Марка: `docs/product-context.md`, `docs/changelog.md`, `docs/roadmap.md`, `docs/status.md`, `docs/team.md`, `docs/team-chat.md`, `docs/decisions/*`, `docs/tasks/*`. `status.md`, `team.md` и общий чат отдельно поручены владельцем. Приложение, миграции, CI/CD, Docker, Railway и Cloudflare в эту область не входят.

## Идентификация перед работой

Каждый агент до начала или возобновления работы указывает в рабочем сообщении и записи задачи:

```text
Agent: Марк
Role: Product Manager
Scope: Состав команды и правила идентификации изменений
```

Scope описывает фактическую область текущей работы. У разработчиков он не заменяется общим «весь проект»; пересечение с другой задачей согласуется до изменения общего участка.

## Идентификация изменений

Обязательные поля присутствуют в **сообщении коммита, описании PR, обновлении статуса задачи и соответствующей записи changelog**:

```text
Agent: Марк
Role: Product Manager
Change: Зафиксированы имена, роли и общий формат записей команды
Related task: docs/tasks/PM-009-team-identity.md
```

- В коммите блок размещается в теле после короткого заголовка; в PR — в описании вместе с результатом и проверками.
- В задаче сохраняется обычный Status/Owner и добавляется запись с датой, этим блоком и следующим шагом. Перед началом также обязателен Scope.
- В changelog блок относится к конкретному изменению. `Agent` обозначает исполнителя изменения; если запись внёс PM по отчёту другого агента, отдельно указать `Recorded by: Марк / Product Manager`. Не выдавать автора записи за автора реализации.
- Для коммитов backend Related task содержит полный URL задачи в `lelik112/parrot669`, чтобы ссылка не указывала на отсутствующий файл другого репозитория.
- У каждой внесённой агентом части указано его имя. Общий технический GitHub-аккаунт не заменяет эти поля и не доказывает личность исполнителя.
- Старые коммиты не переписываются ради нового формата. Историческое авторство дополняется только при наличии основания; неизвестный исполнитель остаётся неизвестным.

Git author можно настроить на выбранное имя, если используемый способ публикации это поддерживает. Обязательная идентификация в тексте действует в любом случае. Отдельные GitHub-аккаунты, новые email и глобальные Git-настройки этим соглашением не создаются.

## Память команды

После значимого изменения PM обновляет **product-context, changelog и decision log**. Запись связывает исполнителя, проблему, выбранное решение, причины и задачу. Для работы без нового продуктового решения делается ссылка на существующее решение; новое решение не выдумывается ради заполнения журнала.

Реализовано, опубликовано и проверено QA — отдельные факты с отдельными основаниями. Статусы задач и [общий статус](status.md) не повышаются из-за одного коммита или нового имени в команде.

Предложения Алекса, переданные владельцем, сохраняют его авторство, но не считаются принятыми до явной фиксации решения. Марк не пишет ответы от имени Алекса, Дениса, Бориса или второго разработчика.

Обсуждение ведётся в соответствующей задаче по [порядку работы](tasks/README.md). Коммит не запускает и не уведомляет автоматически агента; прочтение и начало работы подтверждаются его собственной записью.

Свободные обсуждения — [курилка команды](team-chat.md), созданная по просьбе Алексея. Можно общаться напрямую, обсуждать идеи, процессы и начальство. При старте/возобновлении и перед передачей результата читать новые адресованные сообщения. Рабочие итоги переносить в задачу/decision log; реплики подписывать только своим именем. Для общего разговора без задачи допустимо `Related task: none — docs/team-chat.md` в коммите. Назначение PM становится начатой работой после собственного claim исполнителя.

## Основание

[D004 — состав команды и формат идентификации](decisions/D004-team-identity.md), [PM-009](tasks/PM-009-team-identity.md). Состав и правила подтверждены поручением владельца от 2026-09-24; текст предложения подготовлен Алексом и передан владельцем.
